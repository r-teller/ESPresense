# Handoff — session state log
# Captures where work stands at end of each session.
# gogogo/leroy reads entries[0]; wrapup prepends a new entry.
# IMPORTANT: Always quote commit hashes — e.g. ["abc1234"] not [abc1234].

template_entry:
  date: "YYYY-MM-DD HH:MM UTC"
  branch: "feature/work-YYYYMMDD-HHMM-short-description"
  focus: [feature, bugfix, refactor, infrastructure, documentation, stabilization]
  completed:
    - "bead-id: what was accomplished"
  discovered:
    - "bead-id: new feature/bug filed this session"
  in_progress:
    - "bead-id: current state of partial work"
  blockers:
    - "bead-id: what's stuck and why"
  next_steps:
    - "bead-id: what to do next, in priority order"
  epic_progress: "epic-id XX% → YY%"
  commits: ["abc1234", "def5678"]

entries:
  - date: "2026-05-02 19:30 UTC"
    branch: "feature/athom-plug-v3"
    focus: [stabilization, infrastructure, documentation]
    completed:
      - "ESPresense-gww: closed — pinned click<8.2 in scripts/requirements-pio.txt; all 6 previously-failing c6/s3 envs now build clean; full local matrix 16/16 PASS"
      - "Confirmed upstream ESPresense CI already does the same click<8.2 pin inline (build.yml:36-39) — our solution matches established workaround"
      - "Updated architecture.md PlatformIO health-check row to reference requirements-pio.txt from venv bootstrap step"
      - "Updated upstream-merge-scope memory to add scripts/requirements-pio.txt to exclusion list (user decision: don't propose CI workflow refactor in the Athom PR)"
      - "enhancements.md: marked PlatformIO PATH gap RESOLVED; added 3 new entries covering bootloader rollback limitation, ESPHome→ESPresense partition mismatch, and close-out doc-writing calibration"
    discovered:
      - "Bootloader rollback is not active in our build (CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE=unset in arduino-esp32 pre-built bootloader). Application-side hooks exist (Updater.cpp:249 MarkOtaSuccess + arduino-esp32 initArduino) but bootloader doesn't enforce trial-window. Enabling requires IDF-as-component build mode — deferred until we have a concrete trigger."
      - "Partition table mismatch between Athom factory ESPHome and ESPresense: ESPHome's web OTA only writes the app slot, leaving its partition table in place. ESPresense's SPIFFS partition doesn't exist in ESPHome's typical layout. Documented in docs/install-athom-from-esphome.md Path B with detection guidance."
    in_progress: []
    blockers: []
    next_steps:
      - "Phase 3 — Hardware testing (epic ESPresense-8s8, 1/10 closed). Run scribe-refine on the 9 open beads. Recommended flash path: Tasmota intermediate per docs/install-athom-from-esphome.md (avoids partition mismatch)."
      - "Phase 4 — PR submission (epic ESPresense-cly, 1/7 closed). Defer until Phase 3 complete. Upstream PR exclusion list now covers .claude/, .beads/, .archive/, PRD/, scripts/requirements-pio.txt; docs/install-athom-from-esphome.md is a confirm-with-user item."
      - "Optional future: enable CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE via IDF-as-component switch (~half-day work). Defer until needed."
    epic_progress: "ESPresense-k56 100% (10/10) — no change since last entry; this round was post-Phase-2 toolchain work"
    commits: ["2709281"]

  - date: "2026-05-02 09:00 UTC"
    branch: "feature/athom-plug-v3"
    focus: [stabilization, documentation]
    completed:
      - "ESPresense-k56.7: Boot-trace verified — Relay::EarlyInit at main.cpp:644 precedes Serial.begin (boot-glitch invariant preserved)"
      - "ESPresense-k56.8: 18 cross-module Relay::/CSE7766:: refs all #ifdef-guarded (audited GUI.cpp, main.cpp, both modules' interactions)"
      - "ESPresense-k56.6: UI build deterministic — empty git diff on consecutive npm run build"
      - "ESPresense-k56.1: athom-smart-plug-v3 builds clean (Flash 61.3% / RAM 14.4%, only upstream NeoPixel LTO warning)"
      - "ESPresense-k56.3: Firmware size captured for PR (1,205,972 / 1,966,080 bytes flash; 47,136 / 327,680 bytes RAM)"
      - "ESPresense-k56.5: pio check (cppcheck + clangtidy) PASSED — only stylistic medium findings on Phase 1 files (C-style casts, const-correctness, missing braces, math.h vs cmath); no errors, no memory issues"
      - "ESPresense-k56.2: PARTIAL — 10/16 envs PASS locally; 6 envs (esp32c6/s3 family) fail with environmental Python TypeError in pio bootloader script (NOT a Phase 1 regression, filed gww)"
      - "ESPresense-k56.9: Closed as duplicate of 8s8.1 (both inspect /wifi/hardware endpoint; deferred to Phase 3)"
      - "ESPresense-k56.10: Phase 2 close-out — PRD/README.md status updated, changelog v0.3.0, install guide published at docs/install-athom-from-esphome.md"
      - "Phase 2 epic ESPresense-k56 closed at 100% (10/10)"
      - "docs/install-athom-from-esphome.md published — covers Tasmota intermediate (recommended), direct ESPHome OTA (with partition-mismatch risk explanation), and USB recovery; includes pre-flash checklist, first-boot config, verification table, and known-limitations section"
    discovered:
      - "ESPresense-gww (P2 bug): pio bootloader.bin generation TypeError on esp32c6/s3 envs (Python click API mismatch). Pre-existing; CI is authoritative for those envs."
      - "Pre-flash verification deepens: esptool image_info validates 5 segments + checksum; CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE is unset in arduino-esp32 pre-built bootloader (rollback not active despite Updater.cpp having mark-valid call ready); partition table layout mismatch with typical ESPHome on Athom V3 (ESPresense has eeprom + spiffs partitions; ESPHome typically uses littlefs only) — direct ESPHome OTA leaves partition table in place, causing SPIFFS mount failure post-boot."
      - "Hardware testing strategy clarified: M5StickC Plus is best dual-purpose device (existing m5stickc-plus env, canonical Marauder platform) but doesn't 1:1 validate the C3 binary; recommend pairing with $4 generic ESP32-C3 dev board for binary-exact pre-flash test."
    in_progress: []
    blockers: []
    next_steps:
      - "Phase 3 — Hardware testing (epic ESPresense-8s8, 1/10 closed). Beads need scribe-refine before claim. Recommended order: flash via Tasmota intermediate path per docs/install-athom-from-esphome.md, then walk 8s8.1 (/wifi/hardware) → 8s8.2-9 (relay, button, CSE7766, MQTT, captive portal) → 8s8.10 close-out."
      - "Phase 4 — PR submission (epic ESPresense-cly, 1/7 closed). Defer until Phase 3 complete. The upstream PR scope memory excludes .claude/, .beads/, .archive/, PRD/."
      - "ESPresense-gww (P2): consider pinning click==8.1.7 in .venv-pio or upgrading PlatformIO core to fix local c6/s3 builds. Not blocking."
      - "Optional: enable CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE by switching to IDF-as-component build mode. ~half-day work, would meaningfully improve OTA safety. Defer until needed."
    epic_progress: "ESPresense-k56 10% (1/10) → 100% (10/10)"
    commits: ["322cedb"]

  - date: "2026-05-02 06:30 UTC"
    branch: "feature/athom-plug-v3"
    focus: [feature, infrastructure, documentation]
    completed:
      - "ESPresense-nf5.10: CI matrix entry for athom-smart-plug-v3 (commit baea352)"
      - "ESPresense-nf5.13: Button.cpp consumes DEFAULT_BUTTON1_TYPE / DEFAULT_BUTTON1_PIN macros (commit 511f937)"
      - "ESPresense-nf5.9: Button → GUI dispatch (tap → Relay::toggle, 4s hold → SPIFFS.format + restart) + GUI::ButtonPressed/ButtonLongPressed dispatchers (commit c90457a)"
      - "ESPresense-nf5.11: UI hardware page Relay + Power Monitor sections (commit 73240ba) and regenerated ui_*.h headers (commit 0d155b9)"
      - "ESPresense-nf5.14: PRD/README.md status section updated to mark Phase 1 ✅; architecture.md health checks gained UI deps row; enhancements.md updated"
      - "Phase 1 epic ESPresense-nf5 closed at 14/14 (100%)"
    discovered:
      - "PRD § Step 13 verbatim Svelte block had transcription artifact: {.defaults[...]} should be {$hardwareSettings.defaults[...]}. Fixed inline before pasting; logged in enhancements.md as a scribe-refine improvement target."
      - "ui/node_modules not tracked → npm run build fails fresh. Added health-check row to architecture.md (RESOLVED)."
    in_progress: []
    blockers: []
    next_steps:
      - "Phase 2 — Build verification (epic ESPresense-k56, 1/10 closed). Refine the 9 open beads via scribe-refine, then execute on a fresh session."
      - "Phase 3 — Hardware testing (epic ESPresense-8s8, 1/10 closed). Requires physical Athom plug; flash athom-smart-plug-v3.bin and walk acceptance criteria from nf5.9 (button tap/hold), nf5.11 (captive portal renders, defaults populate), nf5.6/.7 (relay + power monitor live)."
      - "Phase 4 — PR submission (epic ESPresense-cly, 1/7 closed). Defer until Phase 2/3 complete; the upstream PR scope memory excludes .claude/, .beads/, .archive/, PRD/."
      - "Optional: backfill scribe-refine on closed nf5.6/.7 to inline historical implementation contract (cleanliness only — not blocking)."
    epic_progress: "ESPresense-nf5 64% (9/14) → 100% (14/14)"
    commits: ["baea352", "511f937", "c90457a", "73240ba", "0d155b9"]

  - date: "2026-05-01 23:30 UTC"
    branch: "feature/athom-plug-v3"
    focus: [feature, infrastructure, documentation]
    completed:
      - "ESPresense-nf5.1: I2C-on-CDC default GPIO collision fixed (commit 8ae56af)"
      - "ESPresense-nf5.2: JsonDocument buffer 768 → 1024 (folded into 6fbf0f5)"
      - "ESPresense-nf5.3: mqtt discovery helpers extended w/ stateClass + bounded numbers (6fbf0f5)"
      - "ESPresense-nf5.4: ATHOM_PLUG_V3 defaults block added (49a6f62)"
      - "ESPresense-nf5.5: platformio.ini env [env:athom-smart-plug-v3] (f81d179)"
      - "ESPresense-nf5.6: Relay module gated on HAS_RELAY (cdefab8)"
      - "ESPresense-nf5.7: CSE7766 power monitor module gated on HAS_POWER_MONITOR (9f9f121)"
      - "ESPresense-nf5.8: main.cpp lifecycle wiring (9 insertion points + 2 includes) (70aa0cb)"
    discovered:
      - "Vibe-templates upstream gaps — 4 patches in .archive/upstream-patches/ (navigator-recon triage filter, navigator-maintenance + survey triage gates, scribe-init template-aware creation + scribe-refine agent). All 4 also applied locally to .claude/agents/."
      - ".venv-pio/ added to .gitignore — project-local PlatformIO venv (39e1256)"
      - ".claude/enhancements.md created — log of 5 doc gaps from this session"
      - "nf5.11 dependencies formalized: blocked-by nf5.6 + nf5.7 (added mid-session)"
    in_progress: []
    blockers:
      - "nf5.11 (UI hardware page Svelte): still triage:triaged. Now dependency-clear since nf5.6/.7 closed, but bead description is Stub Template only and references PRD § Step 13. Run scribe-refine before claim."
    next_steps:
      - "nf5.13: Update Button.cpp to consume DEFAULT_BUTTON1_* macros (P1, triage:triaged — lightest remaining work, run scribe-refine first)"
      - "nf5.9: Button → GUI dispatch + factory reset (P1, depends on nf5.6/.7 ✓ closed; triage:triaged — needs scribe-refine to inline PRD § Step 8/9 spec)"
      - "nf5.11: UI hardware page Svelte + regenerate ui_*.h (P1, depends on nf5.6/.7 ✓ closed; needs scribe-refine — UI form name= attrs must match firmware HeadlessWiFiSettings keys defined in nf5.6/.7)"
      - "nf5.10: CI matrix entry for athom-smart-plug-v3 in .github/workflows/build.yml (P2, simple chore)"
      - "nf5.14: Update PRD status + close-out Phase 1 (depends on nf5.11)"
      - "Optional: backfill scribe-refine on closed nf5.6/.7 to inline implementation contract for historical record (cleanliness only — not blocking)"
    epic_progress: "ESPresense-nf5 7% (1/14) → 64% (9/14)"
    commits: ["8ae56af", "6fbf0f5", "49a6f62", "cdefab8", "9f9f121", "f81d179", "39e1256", "70aa0cb"]

